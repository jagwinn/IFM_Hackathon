# Generate a value with: python3 -c 'import secrets; print(secrets.token_hex(32))'
WEBUI_SECRET_KEY=
# Set false only for a fresh, localhost-only single-user demo volume.
WEBUI_AUTH=true
# No model API keys are needed for this simulated milestone.
